{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "WRANGLER_BUILD_CONDITIONS=development dotenv -e .dev.vars -- wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=.output",
    "deploy": "wrangler deploy",
    "wrangler": "wrangler",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "typegen": "wrangler types"
  },
  "dependencies": {
    "@hono/zod-validator": "0.4.3",
    "@openmcp/cloudflare": "workspace:*",
    "@openmcp/directory": "workspace:*",
    "hono": "4.7.6",
    "zod": "3.24.3"
  },
  "devDependencies": {
    "@libs/eslint-config": "workspace:*",
    "@libs/tsconfig": "workspace:*",
    "@types/node": "22.14.1",
    "dotenv-cli": "^8.0.0",
    "eslint": "9.25.1",
    "typescript": "5.8.3",
    "wrangler": "4.13.0"
  }
}
